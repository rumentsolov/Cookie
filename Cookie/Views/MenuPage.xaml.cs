using Cookie.Models;
using Cookie.Services;
using System.Collections.ObjectModel;
using System.Linq;
using Cookie.ViewModels;

namespace Cookie.Views
{
    public partial class MenuPage : ContentPage
    {
        public MenuPage()
        {
            InitializeComponent();
            //Binding = new MenuPageViewModels();
        }
    }
}